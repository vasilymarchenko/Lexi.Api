[project]
name = "lexi-api"
version = "0.1.0"
description = ""
authors = [
    {name = "vasilymarchenko"}
]
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "azure-functions",
    "pydantic"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"